[project]
name = "weather-agent"
version = "0.1.0"
description = "ADK Weather Agent"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "python-dotenv",
    "pydantic",
    "google-adk",
    "google-genai",
    "ag-ui-adk",
    "httpx",
]

